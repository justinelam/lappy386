alias pipdate3="pip3 list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1  | xargs -n1 pip3 install -U"

alias jpip="`find /usr/local/Cellar/jupyterlab | grep /bin/python3$` -m pip"
alias jpipdate3="`find /usr/local/Cellar/jupyterlab | grep /bin/python3$` -m pip list --outdated --format=freeze | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 `find /usr/local/Cellar/jupyterlab | grep /bin/python3$` -m pip install -U"
alias jenv="`find /usr/local/Cellar/jupyterlab | grep /bin/python3$` -m venv"
alias awssh="ssh -i "~/.ssh/funkier.pem" ec2-user@ec2-54-191-161-123.us-west-2.compute.amazonaws.com"
alias keyenv="export SSH_PRV_KEY=\$(openssl rsa -in ~/.ssh/id_rsa)"

## PROMPT='%F{red}%B ★ %b%F{cyan}%~%f %# '

eval "$(starship init zsh)"

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
export PATH="/usr/local/sbin:$PATH"

# Google + Android 
export PROJECT_ID=silicon-shape-277704
source "/usr/local/Caskroom/google-cloud-sdk/latest/google-cloud-sdk/path.zsh.inc"
source "/usr/local/Caskroom/google-cloud-sdk/latest/google-cloud-sdk/completion.zsh.inc"
. /usr/local/opt/asdf/asdf.sh

gcloud config set project silicon-shape-277704
export CODE_PASSWORD=$(gcloud secrets versions access latest --secret='CODE_PASSWORD')
export SUDO_PASSWORD=$(gcloud secrets versions access latest --secret='SUDO_PASSWORD')

